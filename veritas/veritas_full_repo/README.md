# Veritas Full Atlas Repo

This refreshed bundle contains:
- `backend/` with the latest available generated backend artifacts from this session
- `frontend/` — Veritas UI (Vite + React + Tailwind). **Atlas API access** collects inquiries; **Atlas admin** reviews them. **Veritas admin** covers validation/HPC operations (in-memory demo until APIs are wired).

## Ports
- Frontend: `7000`
- Backend: `6000`

Install: `cd frontend && npm install && npm run dev`

## Tests included for this refreshed bundle
- backend Python compile check
- Atlas Phase D service smoke test
- Atlas Phase E service smoke test

## Note
This replaces expired download links with a fresh downloadable bundle.
